name: iOS CI
on:
  push:
    branches: [main]

jobs:
  validate:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Validate repository
        run: |
          echo "Skipping iOS build - no valid Xcode scheme configured"
          ls -la
