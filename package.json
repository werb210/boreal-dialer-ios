{
  "name": "boreal-dialer-backend",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test backend/tests/*.test.js",
    "lint": "node -e 'process.exit(0)'",
    "check": "npm test",
    "build": "node -e 'process.exit(0)'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "twilio": "^5.4.0"
  },
  "devDependencies": {
    "supertest": "^7.1.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
