{
  "name": "boreal-dialer-ios",
  "private": true,
  "scripts": {
    "build": "echo \"iOS build requires macOS\"",
    "ios:build": "xcodegen generate && xcodebuild -project BorealDialer.xcodeproj -scheme BorealDialer -sdk iphonesimulator -configuration Debug CODE_SIGNING_ALLOWED=NO build",
    "test:ios": "echo \"iOS build skipped in non-mac environments\""
  }
}
