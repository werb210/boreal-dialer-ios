name: CI

on:
  push:
    branches: [main]

jobs:
  basic-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Repo sanity check
        run: |
          echo "Repo OK"
